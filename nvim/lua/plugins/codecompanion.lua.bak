-- return {
--   -- Remove the broken CodeCompanion
--   -- "olimorris/codecompanion.nvim",
--
--   -- Install CopilotChat with MCP Hub integration
--   {
--     "CopilotC-Nvim/CopilotChat.nvim",
--     branch = "canary",
--     dependencies = {
--       { "zbirenbaum/copilot.lua" }, -- Use this instead of github/copilot.vim
--       { "nvim-lua/plenary.nvim" },
--     },
--     opts = {
--       -- Configure to use your Ollama model
--       model = {
--         provider = "ollama",
--         name = "deepseek-coder:6.7b-instruct-q4_K_M",
--         parameters = {
--           temperature = 0.1,
--         },
--       },
--       -- MCP Hub integration
--       integrations = {
--         mcp = {
--           enabled = true,
--         },
--       },
--     },
--     build = function()
--       vim.notify("Please make sure you have Ollama running with: ollama serve")
--     end,
--     config = function(_, opts)
--       require("CopilotChat").setup(opts)
--
--       -- Keymaps for CopilotChat
--       vim.keymap.set("n", "<leader>ac", "<cmd>CopilotChatToggle<cr>", { desc = "AI Chat Toggle" })
--       vim.keymap.set("x", "<leader>ai", ":CopilotChatVisual<cr>", { desc = "AI Chat Visual" })
--     end,
--   },
-- }
--

return {
  "olimorris/codecompanion.nvim",
  opts = {},
  dependencies = {
    "nvim-lua/plenary.nvim",
  },
},
